services:
  mysql:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      TZ: Asia/Shanghai
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-123456}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-studyforge_ai}
      MYSQL_USER: ${MYSQL_USER:-studyforge}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-123456}
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$${MYSQL_ROOT_PASSWORD} --silent"]
      interval: 10s
      timeout: 5s
      retries: 20

  migrate:
    build:
      context: ../..
      dockerfile: deploy/docker/migrate.Dockerfile
    restart: "no"
    environment:
      DB_CLIENT: mysql
      DB_HOST: mysql
      DB_PORT: "3306"
      DB_NAME: ${MYSQL_DATABASE:-studyforge_ai}
      DB_USER: ${MYSQL_USER:-studyforge}
      DB_PASSWORD: ${MYSQL_PASSWORD:-123456}
      CREATE_DATABASE: "0"
      RESET_SEED: "1"
    depends_on:
      mysql:
        condition: service_healthy

  api:
    build:
      context: ../..
      dockerfile: deploy/docker/api.Dockerfile
    restart: unless-stopped
    environment:
      TZ: Asia/Shanghai
      JDBC_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE:-studyforge_ai}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
      JDBC_USERNAME: ${MYSQL_USER:-studyforge}
      JDBC_PASSWORD: ${MYSQL_PASSWORD:-123456}
      JDBC_MAXIMUM_POOL_SIZE: ${JDBC_MAXIMUM_POOL_SIZE:-20}
      JDBC_MINIMUM_IDLE: ${JDBC_MINIMUM_IDLE:-4}
      STUDYFORGE_UPLOAD_DIR: /var/lib/studyforge/uploads
    volumes:
      - uploads:/var/lib/studyforge/uploads
    depends_on:
      mysql:
        condition: service_healthy

  web:
    build:
      context: ../..
      dockerfile: deploy/docker/web.Dockerfile
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-7897}:80"
    depends_on:
      - api

volumes:
  mysql-data:
  uploads:
