<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/tx https://www.springframework.org/schema/tx/spring-tx.xsd">

    <context:property-placeholder location="classpath:jdbc.properties" ignore-resource-not-found="true"/>
    <context:component-scan base-package="com.studyforge"/>

    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource">
        <property name="driverClassName" value="${JDBC_DRIVER:com.mysql.cj.jdbc.Driver}"/>
        <property name="jdbcUrl" value="${JDBC_URL:jdbc:mysql://localhost:3306/studyforge_ai?useUnicode=true&amp;characterEncoding=utf8&amp;serverTimezone=Asia/Shanghai}"/>
        <property name="username" value="${JDBC_USERNAME:studyforge}"/>
        <property name="password" value="${JDBC_PASSWORD:}"/>
        <property name="maximumPoolSize" value="${JDBC_MAXIMUM_POOL_SIZE:10}"/>
        <property name="minimumIdle" value="${JDBC_MINIMUM_IDLE:2}"/>
    </bean>

    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="configLocation" value="classpath:mybatis-config.xml"/>
        <property name="mapperLocations" value="classpath*:mybatis/**/*.xml"/>
    </bean>

    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <tx:annotation-driven transaction-manager="transactionManager"/>

    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage"
                  value="com.studyforge.system.mapper,
                         com.studyforge.content.mapper,
                         com.studyforge.interaction.mapper,
                         com.studyforge.ai.mapper,
                         com.studyforge.voice.mapper,
                         com.studyforge.help.mapper,
                         com.studyforge.admin.mapper"/>
    </bean>
</beans>
